import React from 'react'

const Footer = () => {
    return <div className="container-footer">
        <div className="text-center">Test App: Map</div>
    </div>
}

export default React.memo(Footer)